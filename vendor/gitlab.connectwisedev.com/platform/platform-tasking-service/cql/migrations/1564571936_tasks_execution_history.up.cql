//
// ATTENTION!!! DO NOT EDIT THIS FILE. IT IS CREATED FOLLOWING SCHEMA MIGRATION STRATEGY
//

CREATE TABLE IF NOT EXISTS tasks_execution_history (
    exec_year      text,
    exec_month     text,
    date           text,
    executed_time  timestamp,
    endpoint_id    text,
    script_name    text,
    completed_time timestamp,
    script_id      text,
    status         text,
    partner_id     text,
    site_id        text,
    machine_name   text,
    executed_by    text,
    PRIMARY KEY ((exec_year, exec_month), date, executed_time, endpoint_id, script_name)
) WITH CLUSTERING ORDER BY (date DESC, executed_time DESC);
